import React from 'react';
import './styles/loading.css';

const Loading = (orops) => {
    return (
        <section className="loading">
           <h2>OMG... its "Loading" , Please Wait !</h2>
        </section>
    )
}

export default Loading;